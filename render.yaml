services:
  - type: web
    name: algoritmo-humano-backend
    env: node
    buildCommand: npm install
    startCommand: DATABASE_URL=file:./dev.db npm run prisma:generate && npm run prisma:migrate && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true